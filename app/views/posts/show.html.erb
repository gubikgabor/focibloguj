<h2><%= @post.title %></h2>
<p><%= @post.body %></p>
<hr>
<%= link_to t('update'), edit_post_path(@post), :class => 'btn btn-primary' %>
<%= link_to t('delete'), post_path(@post),method: :delete, data: {confirm: t('confirm')}, :class => 'btn btn-danger' %>
<hr>
<%= render 'comments/comments' %>
<%= render 'comments/form' %>